% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_model_io.R
\name{save_warm_start_model}
\alias{save_warm_start_model}
\alias{load_warm_start_model}
\title{Save or load a portable warm-start model}
\usage{
save_warm_start_model(model, path, overwrite = FALSE)

load_warm_start_model(
  path = NULL,
  name = NULL,
  source = c("auto", "user", "bundled")
)
}
\arguments{
\item{model}{A valid \link{pairwiseLLM_warm_model} or \code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}} ensemble.}

\item{path}{Explicit file path. Its parent must already exist when saving.}

\item{overwrite}{Allow replacement of an existing artifact. Default FALSE.}

\item{name}{Registered model name, mutually exclusive with \code{path}.}

\item{source}{Registry to search. \code{auto} errors if user and bundled names collide.}
}
\value{
Saving invisibly returns the normalized destination path. Loading returns
the validated model, unchanged from its serialized representation.
}
\description{
Save or load a portable warm-start model
}
\details{
Artifacts are compressed RDS objects, without an envelope or serialized glmnet
engine. Format versions 1 (full audit) and 2 (explicit summary-only) are supported,
independently of package version. Ensembles use their own format 1 and may
contain either supported single-model format. Use \code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}} to add
metadata or explicitly omit audit records before saving. Saving never strips
records or adds timestamps. Neither loading nor prediction from precomputed
features needs glmnet or Python.

Exactly one of \code{path} or \code{name} is required for loading. Positional input means
a path; a missing file never falls back to a registry search. Explicit paths
require \code{source = "auto"}. Registry names follow \code{\link[=register_warm_start_model]{register_warm_start_model()}}.
Load only trusted RDS files; contract validation is not a serialization sandbox.

Writes are staged in the destination directory and validated before publishing.
Failed writes clean up staging files. Replacement uses filesystem rename; if
the platform cannot replace an existing file this way, the operation fails and
leaves that file intact. No persistent backup history is created.
}
\examples{
# A model already fitted from one assessment can be saved explicitly:
if (FALSE) {
  path <- tempfile(fileext = ".rds")
  save_warm_start_model(model, path)
  restored <- load_warm_start_model(path)
  unlink(path)
}
}
