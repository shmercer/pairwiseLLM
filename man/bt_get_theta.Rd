% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_accessors.R
\name{bt_get_theta}
\alias{bt_get_theta}
\title{Extract the final theta table from a run}
\usage{
bt_get_theta(run)
}
\arguments{
\item{run}{A run object returned by \code{\link[=bt_run_adaptive]{bt_run_adaptive()}},
\code{\link[=bt_run_core_linking]{bt_run_core_linking()}}, or \code{\link[=bt_run_adaptive_core_linking]{bt_run_adaptive_core_linking()}} (or a
similarly-structured list).}
}
\value{
A tibble of theta estimates (typically containing at least \code{ID} and
\code{theta}) or \code{NULL} when unavailable.
}
\description{
The \code{pairwiseLLM} runner functions return a list with several tables.
This accessor returns the \code{theta} table when present (converted to a tibble),
or \code{NULL} if the run did not compute theta (e.g., early exit).
}
\examples{
run <- list(
  theta = tibble::tibble(ID = c("A", "B"), theta = c(0.5, -0.5)),
  estimates = NULL,
  pairing_diagnostics = NULL
)
class(run) <- "pairwiseLLM_run"

bt_get_theta(run)

# Missing theta -> NULL
run$theta <- NULL
bt_get_theta(run)

}
\seealso{
\code{\link[=bt_run_adaptive]{bt_run_adaptive()}}, \code{\link[=bt_run_core_linking]{bt_run_core_linking()}},
\code{\link[=bt_run_adaptive_core_linking]{bt_run_adaptive_core_linking()}}.
}
