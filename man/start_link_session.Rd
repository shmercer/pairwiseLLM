% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_session.R
\name{start_link_session}
\alias{start_link_session}
\alias{resume_link_session}
\title{Run and resume explicit-evidence linking sessions}
\usage{
start_link_session(input, status = "active")

resume_link_session(state, input, status = NULL)
}
\arguments{
\item{input}{A \code{\link[=prepare_link_input]{prepare_link_input()}} object, or (for \code{start_link_session})
a list of such objects with distinct spokes and identical hub evidence/judge.}

\item{status}{Explicit reporting status: \code{active}, \code{probe}, or \code{frozen}.
This labels controller status only: all observations in \code{input$cross} remain
active estimation evidence. Held-out probe outcomes must not enter that table.}

\item{state}{A linking session from \code{start_link_session()} or \code{load_link_session()}.}
}
\value{
A \code{pairwiseLLM_link_session} with exact data-only results and a stage log.
}
\description{
Sessions retain common E1--E3 results under \code{linking$estimator}, including
estimator ID/version, per-spoke continuation, diagnostics and exact provenance.
Pair selection and stopping remain external. No estimator is selected by default.
}
\details{
Resume requires the same estimator, version, item order, Phase A,
judge, numerical configuration and unchanged old cross-evidence prefix.
New rows may only be appended. Passing the identical input is an exact no-op
(including MCMC); extended evidence refits using the saved numerical mode,
never the previous posterior as a new prior. A frozen spoke cannot append
evidence until the caller explicitly sets \code{status = "active"}.

With multiple spokes, each estimator fits its own hub/spoke pair. E2/E3 hub
posteriors can differ across spokes. Item summaries retain \code{link_spoke_id}
and do not average or silently select one hub posterior. Ranks are per fit.
}
\seealso{
\code{\link[=prepare_link_input]{prepare_link_input()}}, \code{\link[=fit_link]{fit_link()}}, \code{\link[=start_link_session]{start_link_session()}}, \code{\link[=save_link_session]{save_link_session()}}

Other linking:
\code{\link[=fit_link]{fit_link()}},
\code{\link[=predict_link]{predict_link()}},
\code{\link[=prepare_link_input]{prepare_link_input()}},
\code{\link[=save_link_session]{save_link_session()}},
\code{\link[=summary.pairwiseLLM_link_result]{summary.pairwiseLLM_link_result()}}
}
\concept{linking}
