% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vertex_live.R
\name{submit_vertex_pairs_live}
\alias{submit_vertex_pairs_live}
\title{Live Vertex AI Gemini comparisons for a tibble of pairs}
\usage{
submit_vertex_pairs_live(
  pairs,
  model,
  trait_name,
  trait_description,
  prompt_template = set_prompt_template(),
  api_key = NULL,
  temperature = NULL,
  top_p = NULL,
  top_k = NULL,
  max_output_tokens = NULL,
  thinking_level = NULL,
  thinking_budget = NULL,
  service_tier = "standard",
  api_version = "v1",
  verbose = TRUE,
  status_every = 1L,
  progress = TRUE,
  include_raw = FALSE,
  include_thoughts = FALSE,
  save_path = NULL,
  parallel = FALSE,
  workers = 1,
  ...
)
}
\arguments{
\item{pairs}{Tibble/data frame with columns \code{ID1}, \code{text1}, \code{ID2}, \code{text2}.}

\item{model}{Vertex Gemini model name (for example \code{"gemini-2.5-flash"}).}

\item{trait_name}{Trait name.}

\item{trait_description}{Trait description.}

\item{prompt_template}{Prompt template string, typically from
\code{\link[=set_prompt_template]{set_prompt_template()}}.}

\item{api_key}{Optional Vertex API key.}

\item{temperature}{Optional numeric temperature; forwarded to
\code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}

\item{top_p}{Optional numeric; forwarded to \code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}

\item{top_k}{Optional numeric; forwarded to \code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}

\item{max_output_tokens}{Optional integer; forwarded to
\code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}

\item{thinking_level}{Optional Gemini 3 thinking level; forwarded to
\code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}

\item{thinking_budget}{Optional integer; forwarded to
\code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}

\item{service_tier}{Vertex AI service tier forwarded to
\code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}

\item{api_version}{API version; default \code{"v1"}.}

\item{verbose}{Logical; print status/timing every \code{status_every} pairs.}

\item{status_every}{Integer; how often to print status (default 1 = every
pair).}

\item{progress}{Logical; show a text progress bar.}

\item{include_raw}{Logical; if \code{TRUE}, each row of the returned tibble will
include a \code{raw_response} list-column with the parsed JSON body.}

\item{include_thoughts}{Logical; if \code{TRUE}, requests explicit reasoning
output and stores it in the \code{thoughts} column of the result.}

\item{save_path}{Character string; optional file path to save results
incrementally. If the file exists, the function reads it to identify and
skip pairs that have already been processed (resume mode). Requires the
\code{readr} package.}

\item{parallel}{Logical; if \code{TRUE}, enables parallel processing using
\code{future.apply}. Requires the \code{future} and \code{future.apply}
packages.}

\item{workers}{Integer; the number of parallel workers to use if
\code{parallel = TRUE}. Defaults to 1.}

\item{...}{Reserved for future extensions; passed through to
\code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}.}
}
\value{
A list containing three elements:
\describe{
\item{results}{A tibble with one row per successfully processed pair.}
\item{failed_pairs}{A tibble containing the rows from \code{pairs} that
failed to process, along with an \code{error_message} column.}
\item{failed_attempts}{A tibble of attempt-level failures separate from
observed outcomes.}
}
}
\description{
This is a row-wise wrapper around \code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}. It takes a
tibble of pairs (\code{ID1} / \code{text1} / \code{ID2} / \code{text2}), submits each pair to the
Vertex AI Gemini API, and collects the results with optional incremental
saving and resume support.
}
\examples{
\dontrun{
data("example_writing_samples", package = "pairwiseLLM")
pairs <- make_pairs(example_writing_samples[1:3, ])
td <- trait_description("overall_quality")
out <- submit_vertex_pairs_live(
  pairs = pairs,
  model = "gemini-3.8-flash",
  trait_name = td$name,
  trait_description = td$description,
  thinking_level = "low",
  parallel = FALSE
)
out$failed_pairs
}

}
\seealso{
\code{\link[=check_llm_api_keys]{check_llm_api_keys()}}, \code{\link[=llm_compare_pair]{llm_compare_pair()}}

Other live backends:
\code{\link[=anthropic_compare_pair_live]{anthropic_compare_pair_live()}},
\code{\link[=check_llm_api_keys]{check_llm_api_keys()}},
\code{\link[=gemini_compare_pair_live]{gemini_compare_pair_live()}},
\code{\link[=llm_compare_pair]{llm_compare_pair()}},
\code{\link[=ollama_compare_pair_live]{ollama_compare_pair_live()}},
\code{\link[=openai_compare_pair_live]{openai_compare_pair_live()}},
\code{\link[=submit_anthropic_pairs_live]{submit_anthropic_pairs_live()}},
\code{\link[=submit_gemini_pairs_live]{submit_gemini_pairs_live()}},
\code{\link[=submit_llm_pairs]{submit_llm_pairs()}},
\code{\link[=submit_ollama_pairs_live]{submit_ollama_pairs_live()}},
\code{\link[=submit_openai_pairs_live]{submit_openai_pairs_live()}},
\code{\link[=submit_together_pairs_live]{submit_together_pairs_live()}},
\code{\link[=together_compare_pair_live]{together_compare_pair_live()}},
\code{\link[=vertex_compare_pair_live]{vertex_compare_pair_live()}}
}
\concept{live backends}
