% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_predictions.R
\name{predict.pairwiseLLM_warm_ensemble}
\alias{predict.pairwiseLLM_warm_ensemble}
\title{Predict with an equal-weight warm-start ensemble}
\usage{
\method{predict}{pairwiseLLM_warm_ensemble}(object, newdata = NULL, ..., texts = NULL, ids = NULL, python = NULL)
}
\arguments{
\item{object}{A valid \code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}} ensemble.}

\item{newdata}{Precomputed frozen feature rows, including \code{item_id} and
\code{warm_start_schema} metadata. Mutually exclusive with \code{texts}.}

\item{...}{Reserved; must be empty.}

\item{texts}{Character texts, paired positionally with explicit \code{ids}.}

\item{ids}{Item IDs for text input only.}

\item{python}{Optional explicitly selected Python interpreter for text extraction
only; see \code{\link[=extract_warm_start_features]{extract_warm_start_features()}}.}
}
\value{
A \code{pairwiseLLM_warm_predictions} tibble in input order with \code{item_id},
one \verb{component_<name>} calibrated prediction column per component,
\code{ensemble_mean}, and \code{ensemble_sd}. SD is the sample SD (denominator k - 1)
across all k calibrated predictions, including for exactly two components.
Attributes \code{warm_start_schema} and \code{warm_start_model} retain schema and
ensemble/component identity and format metadata. \code{component_predictions}
retains complete named single-model results, including raw predictions;
\code{component_columns} maps component names to output columns.
}
\description{
Predict with an equal-weight warm-start ensemble
}
\details{
Text features are extracted once. Each model independently applies its stored
preprocessing and OOF calibration to the same raw feature rows. Original BTL
scales never enter the average; calibration is not applied a second time.
Every component must return exactly the input IDs in order and finite numeric
predictions. A failure names the component; no model or row is dropped.
Precomputed prediction needs neither Python nor glmnet.

Ensemble SD measures between-model disagreement. It is diagnostic, not a
Bayesian prior SD or a calibrated uncertainty estimate. These predictions do
not perform BTL prior conversion. No ensemble validation metric is inferred
from component validation metrics.
}
\examples{
if (requireNamespace("glmnet", quietly = TRUE) &&
    requireNamespace("withr", quietly = TRUE)) {
  local({
    # Synthetic features illustrate the interface, not predictive validity.
    example_features <- function(seed) {
      withr::local_seed(seed)
      fields <- warm_start_feature_schema()$feature
      x <- as.data.frame(matrix(runif(15 * length(fields)), nrow = 15))
      names(x) <- fields
      x$n_tokens <- 11:25
      x$token_length_mean <- 2 + 10 * x$token_length_mean
      x$token_length_std <- 0.2 + x$token_length_std
      x$dale_chall_readability_score <- 5 + 20 * x$dale_chall_readability_score
      x <- data.frame(item_id = as.character(1:15), x)
      attr(x, "warm_start_schema") <- "writing_features_v1"
      x
    }
    features <- example_features(3103)
    theta <- 10 + 0.4 * features$n_tokens - 2 * features$token_length_mean
    # A small alpha grid keeps this example fast; the default has 41 values.
    model <- fit_warm_start_model(features$item_id, theta, "synthetic-a",
      features = features, alpha_grid = c(0, 1))
    features_b <- example_features(3104)
    theta_b <- 30 + features_b$n_tokens - 3 * features_b$token_length_mean
    model_b <- fit_warm_start_model(features_b$item_id, theta_b, "synthetic-b",
      features = features_b, alpha_grid = c(0, 1))
    ensemble <- ensemble_warm_start_models(assessment_a = model, assessment_b = model_b)
    predictions <- predict(ensemble, features)
    head(predictions)
  })
}
}
\seealso{
\code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}}, \code{\link[=summary.pairwiseLLM_warm_predictions]{summary.pairwiseLLM_warm_predictions()}},
\code{\link[=make_warm_start_prior]{make_warm_start_prior()}}

Other adaptive warm start:
\code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}},
\code{\link[=extract_warm_start_features]{extract_warm_start_features()}},
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}},
\code{\link[=make_warm_start_prior]{make_warm_start_prior()}},
\code{\link{pairwiseLLM_warm_model}},
\code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}},
\code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}},
\code{\link[=register_warm_start_model]{register_warm_start_model()}},
\code{\link[=save_warm_start_model]{save_warm_start_model()}},
\code{\link[=summary.pairwiseLLM_warm_ensemble]{summary.pairwiseLLM_warm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_predictions]{summary.pairwiseLLM_warm_predictions()}},
\code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}},
\code{\link[=warm_start_python_status]{warm_start_python_status()}}
}
\concept{adaptive warm start}
