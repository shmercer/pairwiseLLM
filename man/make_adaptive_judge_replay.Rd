% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptive_replay.R
\name{make_adaptive_judge_replay}
\alias{make_adaptive_judge_replay}
\title{Create an offline judge from frozen directed outcomes}
\usage{
make_adaptive_judge_replay(
  outcomes,
  item_ids,
  strict_use = TRUE,
  complete = TRUE
)
}
\arguments{
\item{outcomes}{Data frame with character \code{A_id}, character \code{B_id}, and binary
\code{Y} (one means presented A wins). \code{Y} accepts logical, numeric zero/one, or
character \code{"0"}/\code{"1"}; factors, missing values, and other values are rejected.}

\item{item_ids}{Unique non-blank character IDs for the active panel, with at
least two items. IDs must match the adaptive state's item IDs.}

\item{strict_use}{Logical; reject repeated use of an exact ordered judgment.
Default \code{TRUE}. \code{FALSE} permits repeated lookups for non-study inspection.}

\item{complete}{Logical; require all \code{N * (N - 1)} ordered pairs, including both
orientations of every unordered pair. Default \code{TRUE}. \code{FALSE} permits a
partial table; requests for absent orientations still fail during replay.}
}
\value{
A function \code{judge(A, B, state = NULL, ...)} compatible with
\code{\link[=adaptive_rank_run_live]{adaptive_rank_run_live()}}. A and B are one-row data frames with \code{item_id}.
The result contains \code{is_valid = TRUE}, integer \code{Y}, and
\code{judge_backend = "replay"}. Missing keys and strict reuse raise errors.
}
\description{
Replay the stored result for the exact presented \verb{(A_id, B_id)}. Reverse
judgments are independent stored observations: they are never inferred by
complementing the forward result. No provider calls, random draws, or
step-dependent outcomes are used.
}
\details{
For study runs, set \code{adaptive_config = list(dup_max_obs_relaxed = 2L)} when
creating the adaptive state. This prevents hybrid's relaxed third observation
at selection time. Normal presentation balancing and repeat reversal remain
active. Direct strategies already cap unordered pairs at two observations.

Create a fresh judge for each independent replicate. Strict use records each
successful lookup in the closure, even if the caller subsequently discards the
updated state. The judge is not saved in an adaptive session. To resume, create
a new judge from the same matrix and pass the loaded state to the runner;
strict use also rejects keys already present in that state's committed history.
The matrix and its provenance must be retained separately by the caller.
}
\examples{
ids <- c("a", "b", "c")
outcomes <- expand.grid(A_id = ids, B_id = ids, stringsAsFactors = FALSE)
outcomes <- outcomes[outcomes$A_id != outcomes$B_id, ]
outcomes$Y <- as.integer(outcomes$A_id < outcomes$B_id)
judge <- make_adaptive_judge_replay(outcomes, ids)
state <- adaptive_rank_start(ids, seed = 42,
  adaptive_config = list(dup_max_obs_relaxed = 2L))
state <- adaptive_rank_run_live(state, judge, n_steps = 3L, progress = "none")
}
\seealso{
\code{\link[=validate_adaptive_replay]{validate_adaptive_replay()}}, \code{\link[=adaptive_rank]{adaptive_rank()}}

Other adaptive ranking:
\code{\link[=adaptive_rank]{adaptive_rank()}},
\code{\link[=adaptive_rank_resume]{adaptive_rank_resume()}},
\code{\link[=adaptive_rank_run_live]{adaptive_rank_run_live()}},
\code{\link[=adaptive_rank_start]{adaptive_rank_start()}},
\code{\link[=make_adaptive_judge_llm]{make_adaptive_judge_llm()}},
\code{\link[=summarize_adaptive]{summarize_adaptive()}},
\code{\link[=validate_adaptive_replay]{validate_adaptive_replay()}}
}
\concept{adaptive ranking}
