% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_features.R
\name{extract_warm_start_features}
\alias{extract_warm_start_features}
\title{Extract frozen writing features for warm-start prediction}
\usage{
extract_warm_start_features(
  ids,
  texts,
  schema = "writing_features_v1",
  python = NULL
)
}
\arguments{
\item{ids}{Unique, nonmissing character or finite numeric item IDs. IDs are
returned as character strings; blank IDs are not allowed.}

\item{texts}{A nonempty character vector of the same length as \code{ids}, without
missing values. Empty strings are allowed. Text is never trimmed or normalized.}

\item{schema}{The frozen schema identifier, currently \code{"writing_features_v1"}.}

\item{python}{Optional path to an existing Python interpreter. With \code{NULL}, use
an existing environment selected through reticulate. Automatic environment
creation is disabled. Conflicting interpreter selections require correction
or a fresh R session; this function never switches an initialized interpreter.}
}
\value{
A tibble with character \code{item_id} and the 20 numeric features in schema
order, in the requested ID order. The \code{warm_start_schema} attribute records
the schema identifier. Document-level undefined values remain \code{NA}.
}
\description{
Warm-start prediction uses text features to predict relative writing quality
before collecting pairwise comparisons. This function extracts the frozen
English writing features; it does not train a model or estimate BTL scores.
}
\details{
Extraction requires optional reticulate and the audited Python 3.12.3 stack.
See \code{\link[=warm_start_python_status]{warm_start_python_status()}} and the installed setup instructions located by
\code{system.file("python", "README.md", package = "pairwiseLLM")}.
Software and resources must be installed explicitly before extraction. Package
loading, schema inspection, and later prediction from precomputed features do
not require Python. The tested environment is Linux x86_64; Windows and macOS
have not been validated. Features are not evidence of predictive validity.

The default English spaCy model, resource contents and package versions are
checked before extraction. Missing resources fail without downloads. Entropy
is divided by all spaCy tokens, including punctuation and whitespace, rather
than the filtered \code{n_tokens} feature. Undefined values and valid zeros follow
\code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}}. Zero-vector coherence preserves upstream values
and warnings. No feature is imputed or replaced with zero.
}
\examples{
\dontrun{
# Explicitly provision the documented environment first.
extract_warm_start_features(c("a", "b"), c("A short text.", "Another text."),
  python = "/path/to/venv/bin/python")
}
}
\seealso{
\code{\link[=warm_start_python_status]{warm_start_python_status()}}, \code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}}, \code{\link[=fit_warm_start_model]{fit_warm_start_model()}}

Other adaptive warm start:
\code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}},
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}},
\code{\link[=make_warm_start_prior]{make_warm_start_prior()}},
\code{\link{pairwiseLLM_warm_model}},
\code{\link[=predict.pairwiseLLM_warm_ensemble]{predict.pairwiseLLM_warm_ensemble()}},
\code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}},
\code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}},
\code{\link[=register_warm_start_model]{register_warm_start_model()}},
\code{\link[=save_warm_start_model]{save_warm_start_model()}},
\code{\link[=summary.pairwiseLLM_warm_ensemble]{summary.pairwiseLLM_warm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_predictions]{summary.pairwiseLLM_warm_predictions()}},
\code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}},
\code{\link[=warm_start_python_status]{warm_start_python_status()}}
}
\concept{adaptive warm start}
