% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_prior.R
\name{make_warm_start_prior}
\alias{make_warm_start_prior}
\title{Convert warm-start predictions to Bayesian BTL priors}
\usage{
make_warm_start_prior(predictions, ids = NULL, prior_sd = 0.5)
}
\arguments{
\item{predictions}{Calibrated single-model or ensemble predictions, or finite
numeric scores with names or explicit \code{ids}. Numeric input is an expert choice
of relative prior location, not an automatic calibration method.}

\item{ids}{Active item IDs. For prediction tables and named scores these must
match the input ID set exactly and determine output order. For unnamed scores
they identify input positions. Defaults to the IDs in the input.}

\item{prior_sd}{Positive finite scalar or vector. An unnamed vector follows
input order; a named vector aligns by ID. Defaults to 0.5.}
}
\value{
A version-1 \code{pairwiseLLM_warm_prior} list containing \code{item_id}, \code{scores},
\code{prior_mean}, \code{prior_sd}, compact \code{diagnostics}, \code{provenance}, and an integrity
\code{digest}. Pass this object to \code{\link[=fit_bayes_btl_mcmc]{fit_bayes_btl_mcmc()}} or \code{\link[=adaptive_rank_start]{adaptive_rank_start()}}.
}
\description{
Convert warm-start predictions to Bayesian BTL priors
}
\details{
Single-model values must have learned OOF calibration. Ensembles use their
equal-weight calibrated mean. Scores are centered over the active items in R;
adaptive scoped refits subset the saved scores and center again in that scope.
Calibration is not applied twice and original training BTL units are not used.
Ensemble component predictions and sample disagreement SD remain diagnostics;
disagreement never supplies the Bayesian prior SD automatically.

The normal prior applies to \code{theta_raw}; Stan centers this to obtain \code{theta}.
Centering induces dependence, so the supplied SD is not the marginal SD of
centered theta. Without predictive input, BTL retains raw prior mean 0 and SD 1.
This estimation prior is separate from initial warm-start pairing schedules.
}
\examples{
prior <- make_warm_start_prior(c(a = -1, b = 0, c = 1))
prior$prior_mean
}
\seealso{
\code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}}, \code{\link[=predict.pairwiseLLM_warm_ensemble]{predict.pairwiseLLM_warm_ensemble()}},
\code{\link[=fit_bayes_btl_mcmc]{fit_bayes_btl_mcmc()}}, \code{\link[=adaptive_rank_start]{adaptive_rank_start()}}, \code{\link[=adaptive_rank_resume]{adaptive_rank_resume()}}

Other adaptive warm start:
\code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}},
\code{\link[=extract_warm_start_features]{extract_warm_start_features()}},
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}},
\code{\link{pairwiseLLM_warm_model}},
\code{\link[=predict.pairwiseLLM_warm_ensemble]{predict.pairwiseLLM_warm_ensemble()}},
\code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}},
\code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}},
\code{\link[=register_warm_start_model]{register_warm_start_model()}},
\code{\link[=save_warm_start_model]{save_warm_start_model()}},
\code{\link[=summary.pairwiseLLM_warm_ensemble]{summary.pairwiseLLM_warm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_predictions]{summary.pairwiseLLM_warm_predictions()}},
\code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}},
\code{\link[=warm_start_python_status]{warm_start_python_status()}}
}
\concept{adaptive warm start}
