% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_feature_schema.R
\name{warm_start_feature_schema}
\alias{warm_start_feature_schema}
\title{Inspect the frozen warm-start writing feature schema}
\usage{
warm_start_feature_schema(schema = "writing_features_v1")
}
\arguments{
\item{schema}{A single schema identifier. Currently only
\code{"writing_features_v1"} is supported.}
}
\value{
A tibble with one row per feature in fixed predictor order. Columns:
\itemize{
\item \code{schema}, \code{position}, \code{feature}: version, integer order, and canonical name.
\item \code{source_package}, \code{source_version}, \code{component}, \code{upstream_field}:
audited upstream mapping.
\item \code{family}, \code{type}, \code{unit}, \code{interpretation}: meaning of the feature.
\item \code{configuration}, \code{definition}, \code{requirements}, \code{missing_behavior}:
fixed calculation settings, formula, capabilities, and undefined values.
\item \code{source_url}: upstream implementation reference.
}
}
\description{
Warm-start prediction uses text features to predict relative writing quality
before collecting pairwise comparisons. This function lists the frozen
predictor definitions; it does not extract features or require Python.
}
\details{
Version 1 describes English writing using TextDescriptives and a supplementary
textstat readability measure. The schema records definitions, not evidence
of predictive validity for any particular writing population.

\code{upstream_entropy_per_token} divides TextDescriptives' entropy by the number
of all spaCy tokens, including punctuation and whitespace tokens. This is an
average of upstream probability-weighted contributions, not normalized
document Shannon entropy or language-model cross-entropy. Its denominator
differs from the filtered \code{n_tokens} feature. Zero tokens give a missing value.

Definitions, settings, membership, and ordering are frozen. Changes require
a new schema identifier rather than silently modifying version 1. Optional
Python software is needed only for future extraction, not schema inspection
or prediction from precomputed features.
}
\examples{
features <- warm_start_feature_schema()
features[c("position", "feature", "family")]
}
