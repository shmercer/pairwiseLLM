% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rubric_reference.R
\name{prepare_linked_rubric_reference}
\alias{prepare_linked_rubric_reference}
\title{Save a standalone Bayesian ranking as a reusable rubric reference}
\usage{
prepare_linked_rubric_reference(
  cj,
  evidence,
  set_id,
  items = NULL,
  trait = NULL,
  provenance = list()
)
}
\arguments{
\item{cj}{A completed \code{\link[=fit_bayes_btl_mcmc]{fit_bayes_btl_mcmc()}} result. With several refits, the
last refit is used. It must record evidence identity at fit time. Older
results must be refitted from their original comparisons first; they can
still be used for ordinary same-set rubric calibration.}

\item{evidence}{The exact comparison rows used for that refit, in the same
results-table format supplied to \code{\link[=fit_bayes_btl_mcmc]{fit_bayes_btl_mcmc()}}. For a subset fit,
supply only that subset. See \code{\link[=build_btl_results_data]{build_btl_results_data()}}.}

\item{set_id}{A single reference-set identifier, as in \code{\link[=prepare_link_input]{prepare_link_input()}}.}

\item{items}{Optional data frame with \code{item_id} and \code{global_item_id}, mapping
every fitted item to a unique ID across sets. When omitted, fitted item IDs
are also used as global IDs.}

\item{trait}{The trait being ranked, such as \code{"organization"}. Required when
the completed result does not already record it.}

\item{provenance}{Optional named list of serializable study/source metadata,
such as \code{source_commit}. Stored separately from computed identity fields.}
}
\value{
A serializable \code{pairwiseLLM_linked_rubric_reference}. Supply it to
\code{\link[=fit_rubric_calibration]{fit_rubric_calibration()}} with \code{calibration_design = "linked_anchors"}.
\code{items} holds global IDs, source IDs, frozen scores and SDs. For linking,
\code{hub} supplies the set/item mapping; \code{points}, \code{posterior_draws}, and
\code{evidence} supply the E1, E2, and E3 hub inputs, respectively. Attach \code{source}
to each input to carry the same computed \code{reference_hash}. \code{judge} contains
the shared bias/lapse settings from the accepted fit. Diagnostics and
reliability are retained; failed diagnostics warn as in ordinary calibration.
}
\description{
Prepare a completed reference ranking before scoring any new samples. This
supports reference rankings fitted from pooled historical comparisons as well
as a single collection of comparisons. The original scores and comparisons
are saved together so later linking can check that it uses the same reference.
}
\details{
The supplied comparisons must match the evidence recorded when the fit was
made. Compatible item names alone are insufficient. Changed comparisons
require a new fit and a new reference. Save the result with \code{\link[=saveRDS]{saveRDS()}} and
restore it with \code{\link[=readRDS]{readRDS()}}; no live sampler is needed for later prediction.

This constructor supports standalone fits with shared judge parameters.
Import-ready adaptive Phase A artifacts continue to work directly with
\code{\link[=fit_rubric_calibration]{fit_rubric_calibration()}}. Neither route changes the original reference
scores to match the distribution of a new set.
}
\seealso{
\code{\link[=prepare_link_input]{prepare_link_input()}}, \code{\link[=fit_rubric_calibration]{fit_rubric_calibration()}}

Other rubric calibration:
\code{\link[=evaluate_rubric_predictions]{evaluate_rubric_predictions()}},
\code{\link[=fit_rubric_calibration]{fit_rubric_calibration()}},
\code{\link[=predict.pairwiseLLM_rubric_calibration]{predict.pairwiseLLM_rubric_calibration()}}
}
\concept{rubric calibration}
