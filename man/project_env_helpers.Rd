% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_env.R
\name{project_env_helpers}
\alias{project_env_helpers}
\title{Print project-level .Renviron instructions for pairwiseLLM}
\arguments{
\item{python}{Path to a Python executable.}

\item{cache_dir}{Directory path to store cached embeddings.}

\item{file}{Path to the project \code{.Renviron} file (used for display in the
printed instructions). Defaults to \code{.Renviron} in the current working
directory.}

\item{overwrite}{Logical. Ignored (kept for backward compatibility).}
}
\value{
The \code{KEY="value"} line to add to the \code{.Renviron} file (invisibly).
}
\description{
These helpers make it easier to keep configuration project-specific (rather
than global) by printing instructions you can copy into a local \code{.Renviron}
file.
}
\details{
Two common settings are:
\itemize{
\item \code{RETICULATE_PYTHON} for the Python executable used by the \code{reticulate}
package (used by the sentence-transformers embedding engine).
\item \code{PAIRWISELLM_EMBEDDINGS_CACHE_DIR} for a persistent embeddings cache
directory used by \code{\link[=compute_text_embeddings]{compute_text_embeddings()}}.
}

After updating a \code{.Renviron}, restart R so the new environment variables are
picked up.
}
\examples{
\dontrun{
# Print instructions for project-specific reticulate python:
set_project_reticulate_python("/path/to/python")

# Print instructions for persisting embeddings caching in this project:
set_project_embeddings_cache_dir("./.cache/pairwiseLLM")
}

}
