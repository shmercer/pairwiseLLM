% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_accessors.R
\name{bt_get_estimates}
\alias{bt_get_estimates}
\title{Extract the final estimates table from a run}
\usage{
bt_get_estimates(run)
}
\arguments{
\item{run}{A run object returned by \code{\link[=bt_run_adaptive]{bt_run_adaptive()}},
\code{\link[=bt_run_core_linking]{bt_run_core_linking()}}, or \code{\link[=bt_run_adaptive_core_linking]{bt_run_adaptive_core_linking()}} (or a
similarly-structured list).}
}
\value{
A tibble of per-ID estimates or \code{NULL} when unavailable.
}
\description{
Many runners can optionally compute a final per-ID \code{estimates} table
(for example via a final refit step). This accessor returns that table when
present (converted to a tibble), or \code{NULL} if it is unavailable.
}
\examples{
run <- list(
  estimates = tibble::tibble(ID = c("A", "B"), theta_rc = c(0.2, -0.2)),
  theta = NULL,
  pairing_diagnostics = NULL
)
class(run) <- "pairwiseLLM_run"

bt_get_estimates(run)

# Missing estimates -> NULL
run$estimates <- NULL
bt_get_estimates(run)

}
\seealso{
\code{\link[=bt_run_adaptive]{bt_run_adaptive()}}, \code{\link[=bt_run_core_linking]{bt_run_core_linking()}},
\code{\link[=bt_run_adaptive_core_linking]{bt_run_adaptive_core_linking()}}.
}
