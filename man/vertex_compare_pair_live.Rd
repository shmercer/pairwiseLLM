% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vertex_live.R
\name{vertex_compare_pair_live}
\alias{vertex_compare_pair_live}
\title{Live Vertex AI Gemini comparison for a single pair of samples}
\usage{
vertex_compare_pair_live(
  ID1,
  text1,
  ID2,
  text2,
  model,
  trait_name,
  trait_description,
  prompt_template = set_prompt_template(),
  api_key = NULL,
  temperature = NULL,
  top_p = NULL,
  top_k = NULL,
  max_output_tokens = NULL,
  thinking_level = NULL,
  thinking_budget = NULL,
  service_tier = "standard",
  api_version = "v1",
  include_raw = FALSE,
  include_thoughts = FALSE,
  pair_uid = NULL,
  ...
)
}
\arguments{
\item{ID1}{Character ID for the first sample.}

\item{text1}{Character containing the first sample text.}

\item{ID2}{Character ID for the second sample.}

\item{text2}{Character containing the second sample text.}

\item{model}{Vertex Gemini model identifier. You may supply either a short
model name such as \code{"gemini-2.5-flash"} or the fully qualified publisher
model resource
\code{"publishers/google/models/gemini-2.5-flash"}.}

\item{trait_name}{Short label for the trait (e.g. \code{"Overall Quality"}).}

\item{trait_description}{Full-text trait / rubric description.}

\item{prompt_template}{Prompt template string, typically from
\code{\link[=set_prompt_template]{set_prompt_template()}}.}

\item{api_key}{Optional Vertex API key (defaults to
\code{Sys.getenv("VERTEX_API_KEY")}).}

\item{temperature}{Optional numeric temperature. If \code{NULL} (default), the
parameter is omitted and Vertex uses the provider default.}

\item{top_p}{Optional nucleus sampling parameter. If \code{NULL}, omitted.}

\item{top_k}{Optional top-k sampling parameter. If \code{NULL}, omitted.}

\item{max_output_tokens}{Optional maximum output token count. If \code{NULL},
omitted.}

\item{thinking_level}{Optional Gemini 3 thinking level. Supported public
values are \code{"minimal"}, \code{"low"}, \code{"medium"}, and \code{"high"}, but exact
support varies by Gemini 3 model family. This parameter is only valid for
Gemini 3 and later models. Do not supply it together with
\code{thinking_budget}.}

\item{thinking_budget}{Optional thinking budget in tokens. If supplied, the
request includes \code{generationConfig$thinkingConfig$thinkingBudget}. For
models earlier than Gemini 3, this is the supported control surface. Do
not supply it together with \code{thinking_level} on Gemini 3 models.}

\item{service_tier}{Vertex AI service tier. Use \code{"standard"} (default) or
\code{NULL} for provider default behavior. Use \code{"flex"} to request the
documented \code{shared} request-type header or \code{"priority"} to request the
documented \code{dedicated} request-type header.}

\item{api_version}{API version to use, default \code{"v1"}.}

\item{include_raw}{Logical; if \code{TRUE}, the returned tibble includes a
\code{raw_response} list-column with the parsed JSON body.}

\item{include_thoughts}{Logical; if \code{TRUE}, requests explicit reasoning
output via \code{generationConfig$thinkingConfig$includeThoughts} and stores the
first returned text part as \code{thoughts} when available.}

\item{pair_uid}{Optional stable per-pair identifier; when supplied, this
value is used verbatim as \code{custom_id} (otherwise \code{custom_id} defaults to
\code{"LIVE_<ID1>_vs_<ID2>"}).}

\item{...}{Reserved for future extensions.}
}
\value{
A tibble with one row and columns:
\itemize{
\item \code{custom_id} - stable ID for the pair (\code{pair_uid} if supplied).
\item \code{ID1}, \code{ID2} - provided sample IDs.
\item \code{model} - model name returned by the API (or the requested model).
\item \code{object_type} - \code{"generateContent"} on success, otherwise \code{NA}.
\item \code{status_code} - HTTP status code (200 on success).
\item \code{error_message} - error message for failures, otherwise \code{NA}.
\item \code{thoughts} - explicit reasoning text if \code{include_thoughts = TRUE} and the
model returns it; otherwise \code{NA}.
\item \code{content} - concatenated text of the assistant's final answer (used to
locate the \verb{<BETTER_SAMPLE>} tag).
\item \code{better_sample} - \code{"SAMPLE_1"}, \code{"SAMPLE_2"}, or \code{NA}.
\item \code{better_id} - \code{ID1} if \code{SAMPLE_1} is chosen,
\code{ID2} if \code{SAMPLE_2}, or \code{NA}.
\item \code{prompt_tokens}, \code{completion_tokens}, \code{total_tokens} - usage counts if
reported by the API, otherwise \code{NA_real_}.
}
}
\description{
This function sends a single pairwise comparison prompt to the Vertex AI
Gemini API using the express-mode REST \code{generateContent} endpoint and parses
the result into a one-row tibble that mirrors the structure used by the other
live backends.
}
\details{
The prompt template should instruct the model to choose exactly one of
SAMPLE_1 or SAMPLE_2 and wrap the decision in \verb{<BETTER_SAMPLE>} tags.
}
