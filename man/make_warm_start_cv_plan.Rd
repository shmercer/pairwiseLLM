% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_cv_plan.R
\name{make_warm_start_cv_plan}
\alias{make_warm_start_cv_plan}
\title{Construct reusable warm-start cross-validation partitions}
\usage{
make_warm_start_cv_plan(
  ids,
  theta,
  task_id,
  seed = 1L,
  outer_folds = 5L,
  inner_folds = 5L
)
}
\arguments{
\item{ids}{Unique item IDs in the same order as \code{theta}.}

\item{theta}{Finite numeric scores from one assessment.}

\item{task_id}{One nonblank assessment label.}

\item{seed}{Integer random seed, default 1.}

\item{outer_folds, inner_folds}{Fold counts, each at least two. Counts are never
reduced automatically; every training split needs three nonconstant outcomes.}
}
\value{
A portable \code{pairwiseLLM_warm_cv_plan} list, format 1. It contains exact
ordered IDs and outcomes, task identity, seed/RNG provenance, named outer and
inner assignments, and an integrity digest. Save with \code{\link[=saveRDS]{saveRDS()}}.
}
\description{
Construct reusable warm-start cross-validation partitions
}
\details{
Partitions use outcome-ranked blocks with randomized ties and fold labels.
Draw order is outer folds, inner folds for outer training sets in fold order,
then full-data inner folds. The caller's RNG kind and seed state are preserved.
Plans are independent of feature schema and engine. A supplied plan is checked
before extraction or fitting and is never regenerated or silently realigned.
Its digest detects accidental changes, not authorship. Plans contain outcomes
and item IDs; they are development evidence, not anonymized artifacts.
}
\examples{
if (requireNamespace("withr", quietly = TRUE)) {
  plan <- make_warm_start_cv_plan(as.character(1:20), seq_len(20), "example")
  plan$outer_foldid
}
}
\seealso{
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}}

Other adaptive warm start:
\code{\link[=ensemble_warm_start_algorithms]{ensemble_warm_start_algorithms()}},
\code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}},
\code{\link[=extract_warm_start_features]{extract_warm_start_features()}},
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}},
\code{\link[=make_warm_start_prior]{make_warm_start_prior()}},
\code{\link{pairwiseLLM_warm_model}},
\code{\link[=predict.pairwiseLLM_warm_algorithm_ensemble]{predict.pairwiseLLM_warm_algorithm_ensemble()}},
\code{\link[=predict.pairwiseLLM_warm_ensemble]{predict.pairwiseLLM_warm_ensemble()}},
\code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}},
\code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}},
\code{\link[=register_warm_start_model]{register_warm_start_model()}},
\code{\link[=save_warm_start_model]{save_warm_start_model()}},
\code{\link[=summary.pairwiseLLM_warm_algorithm_ensemble]{summary.pairwiseLLM_warm_algorithm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_ensemble]{summary.pairwiseLLM_warm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_predictions]{summary.pairwiseLLM_warm_predictions()}},
\code{\link[=warm_start_coefficients]{warm_start_coefficients()}},
\code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}},
\code{\link[=warm_start_python_status]{warm_start_python_status()}}
}
\concept{adaptive warm start}
