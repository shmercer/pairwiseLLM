% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rubric_calibration.R
\name{predict.pairwiseLLM_rubric_calibration}
\alias{predict.pairwiseLLM_rubric_calibration}
\title{Predict rubric scores from a calibration}
\usage{
\method{predict}{pairwiseLLM_rubric_calibration}(object, newdata = NULL, hard_score = c("median", "mode"), ...)
}
\arguments{
\item{object}{A \code{pairwiseLLM_rubric_calibration} object.}

\item{newdata}{Completed CJ result to predict, or \code{NULL} for the original
items. Same-set scoring accepts the original completed result with unchanged
item IDs, exact accepted scores and uncertainty, and matching fit/reference
evidence. Item reordering is allowed; collection provenance does not affect
scoring. Raw tables, independent cohorts, refits, and Phase B targets are not
supported for same-set prediction. For \code{linked_anchors}, \code{NULL} scores the
original reference items; explicit \code{newdata} must be a completed Phase B
result (or its state) on the stored reference scale. It returns only target
items across all spokes, in their input order. Every spoke must have an
accepted Phase B refit and committed active hub-spoke evidence.}

\item{hard_score}{Ordinal hard-score rule: \code{"median"} (default) or \code{"mode"}.
The median is the lowest category whose cumulative probability is at least
0.5; equality at a median cutpoint therefore selects the lower category.
Modal ties select the lowest category. Ordinal output always retains all
category probabilities, both decision rules, and expected level.
Both choices give the same deterministic category for percentile scoring.}

\item{...}{Reserved arguments; currently must be empty.}
}
\value{
For percentile scoring, a tibble with \code{item_id}, accepted source \code{theta},
integer \code{category} in \code{1:K}, original-label \code{rubric_score}, and \code{extrapolated}
(outside the fitted CJ range). Under the original-result restriction,
extrapolation flags are always false. Stored cutpoints are reused unchanged.
No category probabilities or probabilistic summaries are returned.
Both ordinal methods include the same five columns, plus
\code{probabilities}, a list-column of numeric K-vectors named by the original
ordered labels; integer \code{median_category} and \code{modal_category}; and
\code{expected_level = sum(k * P(Y = k))} for internal indices \code{k} in \code{1:K},
regardless of the original labels' numerical spacing. This expected rubric
level is a continuous summary, not a replacement for the ordinal result.
\code{category} and \code{rubric_score} use the requested \code{hard_score} rule.
Ordinal \code{extrapolated} flags use the labeled calibration range, so unlabeled
source items can be extrapolated; endpoints are included in the range.
Stored standardization is reused. Unfitted objects fail clearly.
Linked target output adds \code{set_id} (target spoke), \code{source_item_id},
\code{global_item_id}, and \code{theta_sd} (accepted Phase B uncertainty, possibly
\code{NA}). Its \code{linking} attribute contains normalized \code{reference}, \code{fit_contract},
\code{provenance} (including hub/spoke IDs and linking stage logs), \code{diagnostics},
and \code{reliability}. Provenance estimation/uncertainty methods reflect validated
spoke contracts, including accepted-state reuse. Each is a single string when
all spokes agree, otherwise the distinct strings in spoke order; per-spoke
contracts retain the complete attribution. Failed upstream diagnostics warn and remain available;
accepted scores do not assert adequate precision or successful stopping.
CJ uncertainty is retained as metadata and is not propagated into category
probabilities. Reference predictions and same-set output keep their existing
columns and have no \code{linking} attribute.
}
\description{
Predict rubric scores from a calibration
}
\seealso{
\code{\link[=fit_rubric_calibration]{fit_rubric_calibration()}}, \code{\link[=evaluate_rubric_predictions]{evaluate_rubric_predictions()}}

Other rubric calibration:
\code{\link[=evaluate_rubric_predictions]{evaluate_rubric_predictions()}},
\code{\link[=fit_rubric_calibration]{fit_rubric_calibration()}}
}
\concept{rubric calibration}
