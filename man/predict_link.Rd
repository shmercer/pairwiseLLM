% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_result.R
\name{predict_link}
\alias{predict_link}
\title{Predict oriented hub-spoke comparison probabilities}
\usage{
predict_link(result, pairs)
}
\arguments{
\item{result}{A valid common result returned by \code{\link[=fit_link]{fit_link()}}.}

\item{pairs}{Data frame with \code{observation_id}, \code{A_set}, \code{A_item}, \code{B_set}, and
\code{B_item}, using the identities in \code{\link[=prepare_link_input]{prepare_link_input()}}. No outcomes are
accepted. A and B encode presentation orientation; both directions are legal.}
}
\value{
A double vector of probabilities that presented A wins, aligned with
the input rows. The estimator's prediction hook integrates its uncertainty;
the dispatcher never silently substitutes probabilities at posterior means.
}
\description{
Predict oriented hub-spoke comparison probabilities
}
\details{
Conditional probabilities are
\code{(1-epsilon) * plogis(theta_A-theta_B+beta) + epsilon/2}.
Reversing presentation is complementary only when beta is zero. Prediction
requires no refit or provider calls. Engines are added in subsequent issues.
}
