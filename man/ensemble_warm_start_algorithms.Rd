% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_algorithm_ensemble.R
\name{ensemble_warm_start_algorithms}
\alias{ensemble_warm_start_algorithms}
\title{Average algorithms trained on the same task}
\usage{
ensemble_warm_start_algorithms(...)
}
\arguments{
\item{...}{At least two explicitly named, fully audited format-3
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}} objects. Names must be nonblank and unique. Load
saved components before calling this constructor. Nested ensembles fail.}
}
\value{
A standalone \code{pairwiseLLM_warm_algorithm_ensemble}, format 1, with
\code{artifact_type = "algorithm_ensemble"}, ordered components, common schema,
outcome and CV identity, and honest nested-CV ensemble validation.
}
\description{
Unlike \code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}}, this constructor requires auditable
shared training outcomes and evaluation partitions. Validation averages the
untouched outer-held-out calibrated predictions, not full-fit predictions or
component validation metrics.
}
\details{
Components must share the exact task, ordered IDs, original outcomes, schema,
CV plan and outer observed values/folds. Every component requires learned
\code{oof_linear} calibration. Legacy and reduced models cannot establish the
necessary audit evidence and must not be used to construct a new ensemble.
Deployment averages each component's calibrated prediction with equal weight.
There are no learned weights, automatic component selection or ensemble-level
recalibration. Validation calibration intercept/slope are diagnostics only.
Component names identify members; distinct engine names are not required.

\code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}} can reduce an existing ensemble to summary-only
audit status. Its portable component payloads and identity survive, but the
stored validation summaries cannot then be recomputed. Between-component SD
is diagnostic disagreement, never a Bayesian prior SD.
}
\examples{
if (requireNamespace("glmnet", quietly = TRUE) &&
    requireNamespace("pls", quietly = TRUE) && requireNamespace("withr", quietly = TRUE)) {
  local({
    withr::local_seed(259L)
    fields <- warm_start_feature_schema()$feature
    x <- as.data.frame(matrix(runif(20 * length(fields)), nrow = 20))
    names(x) <- fields
    x$n_tokens <- 11:30
    x <- data.frame(item_id = as.character(1:20), x)
    attr(x, "warm_start_schema") <- "writing_features_v1"
    theta <- x$n_tokens - 3 * x$token_length_mean
    plan <- make_warm_start_cv_plan(x$item_id, theta, "synthetic", seed = 259L)
    en <- fit_warm_start_model(x$item_id, theta, "synthetic", features = x,
      cv_plan = plan, alpha_grid = c(0, 1))
    pls <- fit_warm_start_model(x$item_id, theta, "synthetic", features = x,
      cv_plan = plan, engine = "pls", engine_control = list(ncomp = 1L))
    ensemble <- ensemble_warm_start_algorithms(elastic_net = en, pls = pls)
    print(ensemble)
    summary(ensemble)$validation$metrics
    predictions <- predict(ensemble, x)
    make_warm_start_prior(predictions, prior_sd = 0.5)
  })
}
}
\seealso{
\code{\link[=make_warm_start_cv_plan]{make_warm_start_cv_plan()}}, \code{\link[=make_warm_start_prior]{make_warm_start_prior()}},
\code{\link[=predict.pairwiseLLM_warm_algorithm_ensemble]{predict.pairwiseLLM_warm_algorithm_ensemble()}}

Other adaptive warm start:
\code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}},
\code{\link[=extract_warm_start_features]{extract_warm_start_features()}},
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}},
\code{\link[=make_warm_start_cv_plan]{make_warm_start_cv_plan()}},
\code{\link[=make_warm_start_prior]{make_warm_start_prior()}},
\code{\link{pairwiseLLM_warm_model}},
\code{\link[=predict.pairwiseLLM_warm_algorithm_ensemble]{predict.pairwiseLLM_warm_algorithm_ensemble()}},
\code{\link[=predict.pairwiseLLM_warm_ensemble]{predict.pairwiseLLM_warm_ensemble()}},
\code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}},
\code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}},
\code{\link[=register_warm_start_model]{register_warm_start_model()}},
\code{\link[=save_warm_start_model]{save_warm_start_model()}},
\code{\link[=summary.pairwiseLLM_warm_algorithm_ensemble]{summary.pairwiseLLM_warm_algorithm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_ensemble]{summary.pairwiseLLM_warm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_predictions]{summary.pairwiseLLM_warm_predictions()}},
\code{\link[=warm_start_coefficients]{warm_start_coefficients()}},
\code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}},
\code{\link[=warm_start_python_status]{warm_start_python_status()}}
}
\concept{adaptive warm start}
