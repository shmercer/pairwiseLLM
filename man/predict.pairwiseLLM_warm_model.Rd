% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_predict.R
\name{predict.pairwiseLLM_warm_model}
\alias{predict.pairwiseLLM_warm_model}
\title{Predict relative quality from a portable warm-start model}
\usage{
\method{predict}{pairwiseLLM_warm_model}(object, newdata, ...)
}
\arguments{
\item{object}{A valid \code{pairwiseLLM_warm_model} object.}

\item{newdata}{Precomputed feature rows with \code{item_id}, all original frozen
schema columns, and matching \code{warm_start_schema} metadata. IDs must be unique,
nonmissing and nonblank. Numeric IDs normalize to character. Extra columns
are ignored; required columns cannot be omitted even if training removed them.}

\item{...}{Reserved for future extensions; must be empty.}
}
\value{
A tibble in \code{newdata} row order with character \code{item_id}, numeric
\code{raw_prediction}, and numeric \code{calibrated_prediction}. Raw predictions are on
the within-task standardized outcome scale, not the original BT/BTL scale.
For uncalibrated core fits, calibrated predictions are \code{NA_real_}, never
identity-calibrated substitutes. Attributes \code{warm_start_schema} and
\code{warm_start_model} record schema identity and model metadata (format version,
task ID, outcome definition, and calibration status), respectively.
}
\description{
Predict relative quality from a portable warm-start model
}
\details{
Prediction applies training medians, centers, and sample SDs, followed by the
stored intercept and coefficients. It does not recompute preprocessing, train
a model, load glmnet, initialize Python, or check a Python environment.
Predictions are not calibrated Bayesian prior means or prior standard
deviations. See \link{pairwiseLLM_warm_model} for the portable model contract.
}
