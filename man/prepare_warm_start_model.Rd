% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_model_artifact.R
\name{prepare_warm_start_model}
\alias{prepare_warm_start_model}
\title{Prepare metadata or a summary-only warm-start artifact}
\usage{
prepare_warm_start_model(model, metadata = list(), omit_audit = FALSE)
}
\arguments{
\item{model}{A valid \link{pairwiseLLM_warm_model} or \code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}} ensemble.}

\item{metadata}{Named list with optional scalar character \code{name}, \code{version},
\code{domain}, \code{notes}, \code{license}, \code{prepared_at}, \code{preparation_package_version},
and \code{extraction_provenance} (a named character vector). Supplied fields replace
existing fields. Timestamps use UTC \code{YYYY-MM-DDTHH:MM:SSZ} strings.}

\item{omit_audit}{Explicitly omit row-level development evidence. Default FALSE.}
}
\value{
A prepared copy using the same model class and prediction method.
}
\description{
Prepare metadata or a summary-only warm-start artifact
}
\details{
Preparation adds a preparation timestamp/package version when absent, not a
training date. Extraction provenance defaults to \code{c(status = "unavailable")};
user-supplied records are not independently verified. Schema identity or a
later Python status check is not extraction provenance.

Ordinary preparation preserves audit records. Explicit omission creates format
2 with summary-only audit status, retaining deployment parameters, tuning
settings and nested-validation summaries. IDs, OOF rows, fold records, tuning
traces and contextual warning messages are omitted; warning counts remain.
Summaries cannot be recomputed without the original evidence. An already
reduced artifact cannot recover its audit through this function.

Ensembles retain ensemble format 1; audit omission recursively reduces each
component to model format 2, preserving existing component metadata. Supplied
preparation metadata applies to the ensemble only.

No raw texts are added. Review task labels, notes, domain, and provenance for
restricted information before bundling; this is not a general anonymizer.
User models do not need complete publication metadata. Task-specific outcome
scales remain standardized independently; storage does not link BTL scales.
}
