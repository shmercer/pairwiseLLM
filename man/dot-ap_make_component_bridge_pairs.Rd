% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptive_pairs_bridging.R
\name{.ap_make_component_bridge_pairs}
\alias{.ap_make_component_bridge_pairs}
\title{Construct deterministic component-bridge candidates}
\usage{
.ap_make_component_bridge_pairs(
  graph_state,
  id_vec_theta,
  n_bridge,
  max_lcc = 5L,
  max_other = 5L
)
}
\arguments{
\item{graph_state}{A graph state list from \.graph_state_from_pairs() or
graph_metrics(), containing named vectors \code{degree} and \code{component_id}.}

\item{id_vec_theta}{Character vector of IDs in theta order.}

\item{n_bridge}{Integer number of bridge pairs desired (upper bound).}

\item{max_lcc}{Integer; max LCC endpoints to consider per component.}

\item{max_other}{Integer; max non-LCC endpoints to consider per component.}
}
\value{
A tibble with at least:
\itemize{
\item i_idx, j_idx, anchor_idx, pair_key
\item source, embed_hit, embed_rank, directed
\item score_info, score_need, score_embed, score_total
plus bridge metadata columns.
}
}
\description{
When the observed comparison graph is disconnected, exploration should
explicitly propose cross-component edges rather than relying on chance.
This helper generates a schema-stable candidate tibble of bridge pairs
between the largest connected component (LCC) and other components.
}
\keyword{internal}
