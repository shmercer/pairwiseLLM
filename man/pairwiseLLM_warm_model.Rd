% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start_model.R
\name{pairwiseLLM_warm_model}
\alias{pairwiseLLM_warm_model}
\alias{summary.pairwiseLLM_warm_model}
\alias{print.pairwiseLLM_warm_model}
\title{Portable task-specific warm-start models}
\usage{
\method{summary}{pairwiseLLM_warm_model}(object, ...)

\method{print}{pairwiseLLM_warm_model}(x, ...)
}
\arguments{
\item{object}{A \code{pairwiseLLM_warm_model} object.}

\item{...}{Reserved for future extensions; must be empty.}

\item{x}{A \code{pairwiseLLM_warm_model} object.}
}
\value{
\code{print()} invisibly returns the model. \code{summary()} returns a named
list describing the task, target, preprocessing, hyperparameters, and
calibration status and stored nested-validation metrics when available.
}
\description{
A warm-start model predicts relative quality from frozen writing features
before pairwise comparisons are collected. Each model represents one task or
assessment; unlinked BT/BTL scores from different tasks must not be pooled.
}
\details{
The fixed-hyperparameter core is internal. It does not tune hyperparameters,
run cross-validation, or learn calibration. \code{\link[=fit_warm_start_model]{fit_warm_start_model()}} adds nested
validation and OOF calibration. Core models remain explicitly uncalibrated.

Format version 1 is an S3 list with these deployment fields:
\itemize{
\item \code{format_version}: integer \code{1L}.
\item \code{schema} and \code{features}: frozen schema identity and original feature order,
including predictors removed during training.
\item \code{preprocessing}: original/retained feature names, named removal reasons,
training missing fractions, retained medians, centers, sample SDs, thresholds,
training row count, and SD convention.
\item \code{coefficients} and \code{intercept}: finite linear coefficients on the stored
preprocessed predictor scale, in retained feature order, and intercept.
\item \code{outcome}: \code{definition = "within_task_z"}, original training mean and sample
SD, and \code{sd_convention = "sample"}. The target is \code{(theta - mean) / sd}.
\item \code{calibration}: \code{status = "uncalibrated"}, with \code{intercept = NULL} and
\code{slope = NULL} for core fits. Public fits store \code{status = "oof_linear"},
learned intercept/slope, calibration row count, method, QR tolerance and OOF source.
\item \code{training}: task ID, training row count, requested alpha/lambda, nonzero
coefficient count, engine/version, and package version.
\item \code{tuning} and \code{validation}: NULL for core fits; public fits retain exact
alpha/lambda traces, fold preprocessing, OOF calibration inputs, outer
held-out predictions and metrics, and warning messages. See \code{\link[=fit_warm_start_model]{fit_warm_start_model()}}.
}

Explicit \code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}} audit omission creates format 2 with
\code{audit_status = "summary_only"}. It preserves deployment parameters and
validation summaries but omits row-level evidence. Those summaries cannot be
recomputed from the reduced artifact. Format 1 remains fully audited.

Preprocessing is fitted only on the supplied training rows. It first removes
all-missing columns and columns with missing fraction strictly above 0.20,
then median-imputes remaining columns. It removes constants and columns with
unique-value fraction at most 0.10 AND most-common/second-most-common frequency
ratio strictly above 19. Frequencies are calculated after imputation. Retained
columns are centered and divided by sample SD (denominator \code{n - 1}). Internal
controls permit changing these thresholds; fitted values are stored.
No surviving predictors is an error. With one survivor, an excluded zero
column satisfies glmnet's two-column input requirement only during fitting;
it is never a schema feature or deployment coefficient.

The internal fixed fit requires at least three observations, explicit alpha
in \verb{[0, 1]}, and finite lambda greater than or equal to zero. It uses Gaussian
glmnet with an intercept and \code{standardize = FALSE}, with solver threshold
\code{1e-12} and maximum \code{100000} iterations, directly at the requested lambda.
Outcomes are standardized before fitting. No PCA or feature screening based
on outcomes is used, including when predictors outnumber observations.

Prediction uses only the stored preprocessing and linear coefficients, never
a serialized glmnet object. Python is optional for extraction, and glmnet is
optional for development; neither is needed to inspect or predict from a
deployment object with precomputed features. Schema metadata is an input
contract, not verified extraction provenance or evidence of predictive validity.
}
\examples{
if (requireNamespace("glmnet", quietly = TRUE) &&
    requireNamespace("withr", quietly = TRUE)) {
  local({
    # Synthetic features illustrate the interface, not predictive validity.
    example_features <- function(seed) {
      withr::local_seed(seed)
      fields <- warm_start_feature_schema()$feature
      x <- as.data.frame(matrix(runif(15 * length(fields)), nrow = 15))
      names(x) <- fields
      x$n_tokens <- 11:25
      x$token_length_mean <- 2 + 10 * x$token_length_mean
      x$token_length_std <- 0.2 + x$token_length_std
      x$dale_chall_readability_score <- 5 + 20 * x$dale_chall_readability_score
      x <- data.frame(item_id = as.character(1:15), x)
      attr(x, "warm_start_schema") <- "writing_features_v1"
      x
    }
    features <- example_features(3103)
    theta <- 10 + 0.4 * features$n_tokens - 2 * features$token_length_mean
    # A small alpha grid keeps this example fast; the default has 41 values.
    model <- fit_warm_start_model(features$item_id, theta, "synthetic-a",
      features = features, alpha_grid = c(0, 1))
    print(model)
    summary(model)
  })
}
}
\seealso{
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}}, \code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}},
\code{\link[=warm_start_coefficients]{warm_start_coefficients()}}, \code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}}

Other adaptive warm start:
\code{\link[=ensemble_warm_start_models]{ensemble_warm_start_models()}},
\code{\link[=extract_warm_start_features]{extract_warm_start_features()}},
\code{\link[=fit_warm_start_model]{fit_warm_start_model()}},
\code{\link[=make_warm_start_prior]{make_warm_start_prior()}},
\code{\link[=predict.pairwiseLLM_warm_ensemble]{predict.pairwiseLLM_warm_ensemble()}},
\code{\link[=predict.pairwiseLLM_warm_model]{predict.pairwiseLLM_warm_model()}},
\code{\link[=prepare_warm_start_model]{prepare_warm_start_model()}},
\code{\link[=register_warm_start_model]{register_warm_start_model()}},
\code{\link[=save_warm_start_model]{save_warm_start_model()}},
\code{\link[=summary.pairwiseLLM_warm_ensemble]{summary.pairwiseLLM_warm_ensemble()}},
\code{\link[=summary.pairwiseLLM_warm_predictions]{summary.pairwiseLLM_warm_predictions()}},
\code{\link[=warm_start_coefficients]{warm_start_coefficients()}},
\code{\link[=warm_start_feature_schema]{warm_start_feature_schema()}},
\code{\link[=warm_start_python_status]{warm_start_python_status()}}
}
\concept{adaptive warm start}
