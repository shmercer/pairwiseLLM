% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bt_stopping.R
\name{bt_should_stop_tier}
\alias{bt_should_stop_tier}
\title{Decide whether to stop using a preset stopping tier}
\usage{
bt_should_stop_tier(
  metrics,
  prev_metrics = NULL,
  tier = c("strong", "good", "very_strong"),
  ...
)
}
\arguments{
\item{metrics}{A one-row tibble returned by \code{\link{bt_stop_metrics}}.}

\item{prev_metrics}{Optional one-row tibble of prior-round metrics (same shape as
\code{metrics}). Used to compute percent improvement for the stability criterion.}

\item{tier}{Stopping tier preset. One of \code{"good"}, \code{"strong"}, \code{"very_strong"}.}

\item{...}{Named overrides for any thresholds accepted by \code{bt_should_stop()}.}
}
\value{
See \code{\link{bt_should_stop}}.
}
\description{
Convenience wrapper around \code{\link{bt_should_stop}} that applies a preset tier
from \code{\link{bt_stop_tiers}} and then applies any explicit overrides supplied
via \code{...}.
}
\examples{
m <- tibble::tibble(
  reliability = 0.88, sepG = 2.6, rel_se_p90 = 0.38,
  item_misfit_prop = 0, judge_misfit_prop = 0
)
bt_should_stop_tier(m, tier = "good")$stop
bt_should_stop_tier(m, tier = "strong")$stop

}
