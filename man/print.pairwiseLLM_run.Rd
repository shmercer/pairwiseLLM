% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print_methods.R
\name{print.pairwiseLLM_run}
\alias{print.pairwiseLLM_run}
\title{Print a pairwiseLLM run}
\usage{
\method{print}{pairwiseLLM_run}(x, ...)
}
\arguments{
\item{x}{A run object returned by \code{\link[=bt_run_adaptive]{bt_run_adaptive()}},
\code{\link[=bt_run_core_linking]{bt_run_core_linking()}}, or \code{\link[=bt_run_adaptive_core_linking]{bt_run_adaptive_core_linking()}} (or a
similarly-structured list).}

\item{...}{Passed to \code{\link[base:print]{base::print()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
This is a minimal print method for objects returned by the \code{pairwiseLLM}
runner functions. It avoids dumping internals and instead shows a small,
stable "headline" view: runner type, number of results, stop reason/round,
theta engine, and (when available) the top few IDs by theta.
}
\examples{
run <- list(
  results = tibble::tibble(ID = c("A", "B"), win = c(1L, 0L)),
  theta = tibble::tibble(ID = c("A", "B"), theta = c(0.5, -0.5)),
  estimates = NULL,
  pairing_diagnostics = NULL,
  stop_reason = "max_rounds",
  stop_round = 2L,
  theta_engine = "bt"
)
class(run) <- "pairwiseLLM_run"

print(run)

}
