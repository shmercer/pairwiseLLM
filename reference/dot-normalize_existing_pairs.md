# Normalize a prior-pairs data frame to ID1/ID2 schema

Normalize a prior-pairs data frame to ID1/ID2 schema

## Usage

``` r
.normalize_existing_pairs(existing_pairs, err_arg = "existing_pairs")
```
